{
  "name": "ryvra-ledger-settlement",
  "version": "0.1.0",
  "description": "Ryvra Ledger Settlement baseline scaffolding",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/**/*.spec.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
